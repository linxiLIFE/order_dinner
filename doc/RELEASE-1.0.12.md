# 1.0.12 发布记录（2026-09-23）

- 新增订单换桌：仅可选择空闲桌台；两小时内的宴席预定在选择列表中标注。原订单和已点菜品保留，原桌台释放，按剩余有效菜品生成新桌台的备菜单打印任务。重复请求不会重复换桌或打印。
- 登录页新增凭旧密码修改密码；修改后旧登录凭据失效。
- 修复已结账订单的原桌台被新订单占用时，撤销重结会撞上唯一占用约束的问题。
- 构建检查：`npm test` 通过（本机未配置测试库时集成项跳过）；另通过 SSH 隧道在隔离 PostgreSQL 测试库执行完整 `test/integration.test.mjs`，1/1 通过。测试库随后已删除。
- 线上发布：`https://dinner.20-48-27-179.sslip.io:1314`，`/healthz` 正常；公开 JS/CSS 与本地 SHA256 一致；`updates/latest.json` 为 1.0.12，两种安装包的线上地址返回 200，远端哈希与清单一致。
- Android 安装包是测试签名，与 1.0.11 测试包证书一致；Windows 安装包未配置代码签名。尚未进行真实设备升级和实体打印机验收。

## 数据清理

执行前备份：`/opt/order-dinner/backups/order-dinner-20260923T055526Z.sql.gz`；部署期间第二份备份：`/opt/order-dinner/backups/order-dinner-20260923T060619Z.sql.gz`。两份均通过 `gzip -t`。

点单服务停机期间运行 `scripts/reset-business-data.sql`，事务已提交，并已重启服务。订单、批次、明细、结账、积分流水、打印任务、宴席预定和定金流水、顾客、操作日志、幂等记录均为 0。保留菜品 173、分类 14、菜品选项组 63、选项 159、桌台 12、设置 11、员工账号 1、打印设备 1、宴会厅 1；占用桌台为 0。

## 服务器文件整理（2026-09-23）

- 只保留当前 1.0.12 发布目录；在线更新目录只含 `latest.json`、1 个 Android APK 和 1 个 Windows EXE。旧的 14 个发布目录、8 个旧更新目录均移入 `/home/azureuser/.local/share/Trash/files`。
- 当前数据库备份为 `/opt/order-dinner/backups/order-dinner-20260923T062239Z-1615708.sql.gz`，SHA256：`87caac03655460a6c92505b8bcf0278930930ee6e2e1499e08063021bd2359b1`，已通过 `gzip -t`。其余 26 份旧备份和第一次新备份均移入服务器废纸篓。
- `scripts/backup.sh` 已改为每次校验新备份后只保留最新一份；当前服务器定时任务使用的脚本已同步。`scripts/deploy.sh` 已改为只上传当前版本安装包，并在新版本健康检查通过后把旧发布目录移入废纸篓。
- 废纸篓仍占用系统盘空间；移动文件后磁盘可用空间约 3.4 GB，未对废纸篓执行永久删除。
