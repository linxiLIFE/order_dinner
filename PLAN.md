# 项目范围与剩余工作

本计划只保留已明确要求的业务范围和当前待修问题。未列出的扩展暂不开发；收窄计划不代表删除已经存在的业务数据或代码。

## 保留的业务范围

- 多设备同步桌台和订单；服务器数据为准，SSE 通知与定时刷新共同兜底。
- 桌台停用与恢复。
- 菜品分类排序。
- 完整 CSV 营业报表，包含菜品销量、每桌平均消费、新客、老客和散客统计。
- 打印设备设置，包括设备启停、当前打印主机、在线状态和重新授权。
- 将 53 道指定菜品写入生产环境；初始化可重复执行，不能覆盖已有菜品的营业价格。
- 其余现有点餐、结账、积分和打印流程作为系统基础，不在本计划中扩展新的业务分支。

## 本轮修复与验证

- [x] App 始终监听 401 登录失效事件；无 token 首次打开、登录后 token 过期都能回到登录页。
- [x] SSE、轮询和手动刷新使用请求序号；较早发出的响应不得覆盖较新的桌台、订单、分类或打印设备状态。
- [x] Android 打印机选择不受旧闭包影响；状态轮询不得覆盖员工刚选择的设备。
- [x] 并发开台遇到已占用桌台时返回 409 冲突，而不是 400。
- [x] 使用本机隔离的 PostgreSQL 测试数据库实际运行 `test/integration.test.mjs`，确认测试没有被跳过；不得连接生产库。
- [x] 运行完整构建和测试，并检查 `git diff --check`。

## 暂不开发

- 菜品图片管理。
- 顾客历史菜品明细、累计消费等扩展档案。
- 目标实收价专门输入、额外减免原因流程。
- 未明确提出的扩展统计、管理页和其他新功能。

## 验收边界

- 自动化测试和构建通过只证明对应代码路径；多设备真实同步、Android 蓝牙打印和生产环境菜单写入仍需按实际环境分别验收。
- 生产菜单初始化必须以迁移/初始化逻辑验证为准；本地测试不得连接或修改生产数据库。
