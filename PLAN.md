# 餐厅员工点单系统实施方案

## 一、交付与界面

交付电脑网页、手机网页、安卓安装包、Windows 安装包，共用订单、会员和营业数据。固定安卓设备通过蓝牙连接打印机，统一处理各端提交的打印任务。

- 界面以白色为底，中文按钮、中文提示、大字号金额；不放英文标题、宣传语和多余说明。
- 主要页面：桌台、点餐、结账、顾客、菜品、营业统计、设置。
- 电脑采用分类、菜品、当前订单三栏布局；手机采用分类切换、菜品列表和底部订单栏。
- 老板管理菜品、成本、统计、人员和设置；收银员可以点餐、查询顾客、赠送、退菜、减免和结账。
- 默认单店、每桌一张进行中的账单；桌号、分类、店名和小票信息均可配置。

## 二、点餐、结账与积分

**点餐流程**

- 输入手机号或选择“散客” → 选择空桌 → 填写人数，默认两人 → 点餐。
- 菜品支持分类选择、中文模糊搜索、拼音和首字母搜索，支持数量和口味备注。
- 提交后保存本次菜品批次，生成两份备菜单；加菜形成新批次，只打印新增菜品。
- 多设备同步桌台和订单；防止重复开桌、重复提交，以及一台结账时另一台继续加菜。
- 断网保留本机草稿，暂停提交和结账；恢复后核对最新订单再提交。

**赠送、退菜与结账**

- 赠送：保留菜名、数量、原价，标记“赠送”，应收为零，成本照计；支持部分数量赠送。
- 退菜：记录数量、原因及是否已制作。未制作不计成本，已制作计入损耗；向厨房打印两份退菜单。
- 结账依次计算：有效菜品原价合计 − 赠送金额 − 人工减免 − 积分抵扣。最终应收不得为负。
- 收银员可填写减免金额或目标实收价；目标实收价转换为明确的减免记录，保留操作人和原因。
- 确认线下收款方式及金额后，完成结账、释放桌台并生成一份结账小票；打印失败可补打，不重复记账。
- 老板可以撤销结账并重结。原账单、冲销和新账单均保留；历史重结不占用当前桌台、不重复通知厨房。收退款差额由工作人员线下处理并记录。

**积分**

- 默认实收每满一元积一分，每十分抵一元；不足部分向下取整，本次所得积分下次使用。
- 老板可修改获取、抵扣比例和开关；旧账单保留当时规则。
- 抵扣不超过可用积分及当前应收金额；积分永久有效，默认无额外抵扣比例限制。
- 结账与积分增减在同一数据库事务完成；撤销时返还已抵积分、收回所得积分。若余额不足以冲回，阻止撤销并显示关联消费记录，不产生负积分。

## 三、菜品、顾客与营业统计

- 菜品包含名称、分类、单位、售价、成本价、可选图片和在售状态；默认按整数份数点单。
- 分类支持增删、排序；删除已被订单引用的菜品采用归档，历史账单继续保留。
- 每次下单保存菜名、售价和成本快照，之后改价不影响历史记录。
- 按手机号查询积分余额、积分流水、历次用餐时间、菜品、实收和累计消费。
- 统计营业额、订单数、用餐人数、每桌平均消费、人均消费、毛利润、毛利率、优惠金额、退菜损耗及菜品销量，支持日期筛选与表格导出。
- 营业额按有效结账实收统计；毛利润为实收减菜品成本及已制作退菜损耗，不扣房租、工资。
- 按北京时间零点划分营业日；新老客占比按有手机号的已结账订单统计，首次有效消费为新客，其后为老客；散客另列。
- 撤销和重结保留操作时间，报表归属原营业日，避免重复计入收入。

## 四、技术、打印与部署

**应用和数据库**

- 使用 React、TypeScript 构建共用界面，Node.js 服务提供业务接口，PostgreSQL 保存业务数据。
- 安卓采用 Capacitor 加原生蓝牙打印模块；Windows 使用 Electron 安装包，默认支持 Windows 10/11 的 64 位系统；安卓默认支持安卓 8 及以上。[安卓集成资料](https://capacitorjs.com/docs/android) · [桌面打包资料](https://www.electron.build/docs/)
- 数据模型包含员工、桌台、顾客、分类、菜品、订单、下单批次、明细快照、结账版本、积分流水、打印任务及操作记录。
- 接口覆盖登录、开桌、加菜、赠送、退菜、结账、撤销重结、顾客查询、统计和打印任务领取回执。
- 金额统一使用整数“分”；服务端计算价格和权限，关键写入使用事务、订单版本和请求唯一标识，避免重复扣分、重复结账。
- 成本和老板报表在接口层限制访问；员工独立账号登录，密码加密保存。

**热敏打印**

- 按截图中的 XP-N160II、80 毫米纸张设计；采用安卓原生蓝牙连接，先验证实际设备协议和中文打印。[安卓蓝牙连接资料](https://developer.android.com/develop/connectivity/bluetooth/connect-bluetooth-devices)
- 固定安卓设备开启打印服务，保持登录及设备连接；其他手机和电脑通过服务器队列交给它打印。
- 备菜单突出桌号、人数、时间、批次、菜品数量和备注，不显示成本或顾客完整手机号。
- 两份备菜单分别记录发送进度；补打标明“补打”。未发送任务可重试，已经发送但结果不明的任务须核对后补打。
- 安卓本地保存已领取任务和发送进度，重启后恢复；单台打印设备串行处理，避免多人同时打印串单。
- 结账小票显示原价、赠送、退菜、各项减免、积分抵扣、实收、收款方式和积分余额。打印状态与订单状态分别保存。

**服务器**

- 部署到独立目录 `/opt/order-dinner`，应用和数据库使用独立容器、独立持久化目录。
- 访问地址采用 `https://dinner.20-48-27-179.sslip.io:1314`。域名解析已核实，现有安全入口可访问；实施时新增该域名路由和证书。
- 保留现有服务及端口配置，数据库不对公网开放；修改入口前备份并验证配置，失败时恢复。
- 每日数据库备份并提供人工下载，执行恢复演练；升级前备份数据，保留上一版本用于回退。
- 提供部署、备份、打印设备配置和安装说明，并维护项目状态文档。所有需要删除的文件移入废纸篓。

## 五、验收标准

- 完整验证：会员或散客开桌 → 点餐打印两份 → 加菜仅打印新增项 → 赠送及退菜 → 减免和积分抵扣 → 收款、打印小票、释放桌台。
- 核对部分数量赠送、全部免费、积分不足、规则修改、历史菜品改价，以及撤销重结后的账单、积分和统计。
- 两台设备并发操作时，不重复开桌、下单、扣分或结账；过期订单提示刷新。
- 验证断网草稿、打印设备离线、蓝牙中断、缺纸、两份只完成一份、应用重启和补打。
- 检查手机与电脑布局、全中文操作界面、收银员权限隔离，以及历史顾客查询和报表对账。
- 分别验收网页、安装包构建、安卓与 Windows 实机运行、真实打印机出纸和线上部署；构建通过不能替代实机验收。
- 最终交付源码、数据库迁移、部署配置、两个安装包、使用说明及验收记录；真实菜单和桌号由老板配置，正式库不混入演示营业数据。
